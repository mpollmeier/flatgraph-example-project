package testdomains.simple.nodes

/** Node types with this marker trait are guaranteed to have the description property. EMT stands for: "erased marker trait", it exists only
  * at compile time in order to improve type safety.
  */
trait HasDescriptionEMT

/** Node types with this marker trait are guaranteed to have the name property. EMT stands for: "erased marker trait", it exists only at
  * compile time in order to improve type safety.
  */
trait HasNameEMT

/** Node types with this marker trait are guaranteed to have the order property. EMT stands for: "erased marker trait", it exists only at
  * compile time in order to improve type safety.
  */
trait HasOrderEMT

/** Node types with this marker trait are guaranteed to have the string_list property. EMT stands for: "erased marker trait", it exists only
  * at compile time in order to improve type safety.
  */
trait HasStringListEMT
